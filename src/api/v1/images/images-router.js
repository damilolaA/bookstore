/*const express = require('express'),
	  router  = express.Router(),
	  controller = require('./images-controller.js');

router.route("/:filename")
	.get(controller.getImages);

module.exports = router;*/